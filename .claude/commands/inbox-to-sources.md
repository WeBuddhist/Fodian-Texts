Read `4-SYSTEM/Skills/inbox-to-sources/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Promote reviewed inbox texts into `1-SOURCES/` — one Tibetan root text per work in `Text/`, one file per distinct Chinese edition in `Translations/`, with parts joined into chapters, `^chapter-verse` block IDs, and each Chinese segment transcluding its Tibetan counterpart.
