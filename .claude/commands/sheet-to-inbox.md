Read `4-SYSTEM/Skills/sheet-to-inbox/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Ingest the "Pecha Upload List" Google Sheet into `0-INBOX/texts/` — one markdown file per text row, with the document body downloaded from its Google Doc link and every sheet column, catalogue ID, and cross-language relation recorded in YAML frontmatter.
